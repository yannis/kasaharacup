!!! 5
- html_tag class: 'no-js', lang: 'en' do
  = render 'head'
  %body{ :class => "#{ controller.controller_name }" }
    = render 'chromeframe'
    / = render 'header'
    / = render 'flashes'
    = yield
    / = render 'footer'
    = render 'javascripts'
