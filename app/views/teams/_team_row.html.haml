%tr
  %td= "#{index})"
  %td= link_to team.name, team_path(team)
  %td=raw team.kenshis.map{|kenshi| link_to kenshi.full_name, kenshi_path(kenshi) }.join(', ')
  %td= team.complete? ? "complete" : "incomplete"
  %td= team.isvalid? ? "valid" : "invalid"
  %td=raw team_admin_links(team)
