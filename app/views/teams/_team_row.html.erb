<li>
  <div class="py-4 px-3 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-lg font-medium text-gray-600 prose">
          <%= team_counter + 1 %>)
          <%= link_to(cup_team_path(team.cup, team)) do %>
            <%= team.name %>
          <% end %>
        </p>
        <p class="text-sm font-medium text-gray-400 truncate">
          <em><%= team.complete? ? t(".complete") : t(".incomplete") %></em>,
          <em><%= team.isvalid? ? t(".valid") : t(".invalid") %></em>
        </p>
      </div>
      <div class="ml-2 shrink-0 flex">
        <%== team_admin_links(team, options: {class: "pull-right"}) %>
      </div>
    </div>
    <div class="mt-6 sm:flex sm:justify-between">
      <div class="sm:flex">
        <p class="flex items-center text-gray-500">
          <!-- Heroicon name: user-group -->
          <svg xmlns="http://www.w3.org/2000/svg" class="shrink-0 mr-1.5 h-5 w-5 text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path d="M13 6a3 3 0 11-6 0 3 3 0 016 0zM18 8a2 2 0 11-4 0 2 2 0 014 0zM14 15a4 4 0 00-8 0v3h8v-3zM6 8a2 2 0 11-4 0 2 2 0 014 0zM16 18v-3a5.972 5.972 0 00-.75-2.906A3.005 3.005 0 0119 15v3h-3zM4.75 12.094A5.973 5.973 0 004 15v3H1v-3a3 3 0 013.75-2.906z" />
          </svg>
          <%= safe_join(team.kenshis.map { |kenshi| link_to kenshi.full_name, cup_kenshi_path(team.cup, kenshi) }, raw("<span class='px-2'>•</span>")) %>
        </p>
      </div>
    </div>
  </div>
</li>
