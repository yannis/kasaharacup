<%# Replaced by Task 21 with the real PoolComponent invocation. %>
<%# Kept here so the broadcast callback can render it during Task 16's tests. %>
<%= turbo_frame_tag pool_dom_id(category, pool_number) do %>
  <p>Pool <%= pool_number %></p>
<% end %>
